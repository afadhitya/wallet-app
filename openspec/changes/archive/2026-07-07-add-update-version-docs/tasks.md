## 1. Update skill/COMMANDS.md

- [x] 1.1 Add a "System" section after the "Init" section documenting `wallet version` (with `--check` flag) and `wallet update` (with `--force` flag)

## 2. Update skill/ERRORS.md

- [x] 2.1 Add 5 update-related error codes to the reference table: `UPDATE_NETWORK_ERROR`, `UPDATE_PERMISSION_ERROR`, `UPDATE_ALREADY_LATEST`, `UPDATE_FAILED`, `UPDATE_CHECKSUM_MISMATCH`

## 3. Update README.md

- [x] 3.1 Add a brief mention of `wallet version` and `wallet update` commands in the README's "Commands" section
