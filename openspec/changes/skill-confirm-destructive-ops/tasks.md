## 1. Update Rules Section

- [x] 1.1 Add confirmation rule for destructive operations to `skill/SKILL.md` Rules section, positioned after the `--force`/`rm` rule and `--` flag separator rule, before "Never touch the database directly"

## 2. Verification

- [x] 2.1 Read the Rules section of `skill/SKILL.md` and confirm the new bullet appears in the correct position with correct wording
